# Runtime state

Each client directory is mounted as that Headroom instance's persistent `~/.headroom` directory.

Runtime contents are ignored by Git; the `.gitkeep` files only preserve the directory layout.
